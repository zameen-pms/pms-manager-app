import styled from "styled-components";

export const StyledTenantForm = styled.form`
	display: flex;
	flex-direction: column;
	gap: 1rem;

	.grid {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 1rem;
	}
`;
