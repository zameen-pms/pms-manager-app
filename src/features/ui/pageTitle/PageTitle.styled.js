import styled from "styled-components";

export const StyledPageTitle = styled.div`
	display: flex;
	flex-direction: row;
	align-items: center;
	gap: 0.5rem;

	svg {
		width: 28px;
		height: 28px;
		cursor: pointer;
	}
`;
